PORT=4173

# Optional Supabase integration
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
READO_LIBRARY_BUCKET=library-sources
# Optional aliases (if you prefer READO_* naming)
READO_SUPABASE_URL=
READO_SUPABASE_ANON_KEY=

# Optional parser webhook (for pdf/epub/mobi)
READO_PARSER_WEBHOOK_URL=
READO_PARSER_WEBHOOK_TOKEN=
# Optional NotebookLM parser bridge (recommended for image-grounded PDF parsing)
# Expect POST JSON: { name, mimeType, contentBase64 } and return JSON containing text/content/pages/chunks
READO_NOTEBOOKLM_BRIDGE_ENDPOINT=
READO_NOTEBOOKLM_BRIDGE_API_KEY=
# Optional PDF Reader MCP bridge endpoint (see scripts/studio-tools/pdf-mcp-bridge.mjs)
READO_PDF_READER_MCP_ENDPOINT=
READO_PDF_READER_MCP_API_KEY=
# Context/file safety limits
READO_MAX_CONTEXT_CHARS=180000
READO_UPLOAD_MAX_MB=25

# Optional MCP search bridge (if empty, fallback to wiki/crossref/openalex)
READO_MCP_SEARCH_ENDPOINT=
READO_MCP_SEARCH_API_KEY=

# Optional MCP web-fetch bridge (recommended for anti-bot/JS-heavy websites)
READO_MCP_FETCH_ENDPOINT=
READO_MCP_FETCH_API_KEY=

# Optional Stitch bridge (HTML provider)
# auto | llm | stitch
READO_HTML_PROVIDER=auto
READO_STITCH_BRIDGE_ENDPOINT=
READO_STITCH_BRIDGE_API_KEY=

# Optional server-side LLM generation
READO_LLM_ENDPOINT=https://api.deepseek.com/chat/completions
READO_LLM_API_KEY=
READO_LLM_MODEL=deepseek-chat
# on/off: when on, each module page is generated as unique HTML by LLM (fallbacks to template if failed)
READO_LLM_HTML_MODE=on
# on/off: when on, generation fails if LLM HTML is invalid (no template fallback)
READO_REQUIRE_LLM_HTML=on

# Optional Codex/OpenAI-style endpoint (takes precedence over READO_LLM_*)
# Example (Responses API): https://api.openai.com/v1/responses
# Example (Chat Completions): https://api.openai.com/v1/chat/completions
READO_CODEX_ENDPOINT=
READO_CODEX_API_KEY=
READO_CODEX_MODEL=gpt-5-mini

# Optional Stripe billing (subscriptions)
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=
STRIPE_PRICING_TABLE_ID=
# Legacy single-plan checkout price (optional when using STRIPE_PRICE_* matrix below)
STRIPE_PRICE_ID=
STRIPE_SUCCESS_URL=
STRIPE_CANCEL_URL=
STRIPE_PORTAL_RETURN_URL=
# Custom checkout matrix for self-hosted pricing UI (SaaS 2.0)
STRIPE_PRICE_MONTHLY_EXPLORER=
STRIPE_PRICE_ANNUAL_EXPLORER=
STRIPE_PRICE_MONTHLY_SCHOLAR=
STRIPE_PRICE_ANNUAL_SCHOLAR=
STRIPE_PRICE_LIFETIME=
STRIPE_PRICE_ADDON_2000=
# Backward-compatible legacy names (optional)
STRIPE_PRICE_MONTHLY_STARTER=
STRIPE_PRICE_MONTHLY_TRIAL=
STRIPE_PRICE_MONTHLY_PRO=
STRIPE_PRICE_ANNUAL_STARTER=
STRIPE_PRICE_ANNUAL_TRIAL=
STRIPE_PRICE_ANNUAL_PRO=
# Example:
# STRIPE_SUCCESS_URL=https://reado.fun/pages/billing-success.html
# STRIPE_CANCEL_URL=https://reado.fun/pages/billing-cancel.html

# Optional server-side credits (daily refresh + monthly grant)
READO_CREDITS_DAILY_FREE=0
READO_CREDITS_MONTHLY_FREE=0
READO_CREDITS_DAILY_SMALL=0
READO_CREDITS_MONTHLY_SMALL=4000
READO_CREDITS_DAILY_LARGE=0
READO_CREDITS_MONTHLY_LARGE=12000
READO_CREDITS_DAILY_LIFETIME=0
READO_CREDITS_MONTHLY_LIFETIME=2000
READO_ADDON_CREDITS_2000=2000
# Optional explicit Stripe price-id mapping to small/large plans (comma-separated)
READO_STRIPE_PRICE_IDS_SMALL=
READO_STRIPE_PRICE_IDS_LARGE=
